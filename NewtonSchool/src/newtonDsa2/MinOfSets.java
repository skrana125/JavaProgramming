package newtonDsa2;

public class MinOfSets
{
    // function to print sum of two sets
    static  void setSums(int n)
    {
        //check the base case
        if(n<=2)
        {
            System.out.println("-1");
            return;
        }
        // Sum of first numbers upto N
        int value = (n * (n + 1)) / 2;


    }
}
